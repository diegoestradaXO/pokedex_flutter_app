import 'package:flutter/material.dart';

class BasePokemonCard extends StatelessWidget {
  const BasePokemonCard({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}